{
  "name": "sistema-atendimento-social",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "serve -s frontend/dist -l $PORT",
    "serve": "serve -s frontend/dist -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18.16.0",
    "npm": ">=9.5.0"
  }
}
